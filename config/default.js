const tags = [{
  value: '默认',
  label: '默认'
}]

const preface = ''

const baseURL = ''

const view = {}

const BAIDU_ANALYSIS_URL = undefined

const websiteName = '时间轴wiki'

const config = {
  tags,
  preface,
  baseURL,
  view,
  BAIDU_ANALYSIS_URL,
  websiteName
}

export default config
