const tags = [{
  value: '默认',
  label: '默认'
}]

const preface = ''

const baseURL = ''

const view = {}

const BAIDU_ANALYSIS_URL = undefined

const config = {
  tags,
  preface,
  baseURL,
  view,
  BAIDU_ANALYSIS_URL
}

export default config
