import defaultConfig from './default'

const preface = '时间轴 Wiki - 指挥信息系统技术与应用'

const websiteName = '指挥信息系统技术与应用'

const config = Object.assign({}, defaultConfig, {
  preface,
  websiteName
})

export default config
